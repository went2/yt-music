<view class="title">
  <view class="left">
    <view class="desc" wx:if="{{ showDesc }}">{{ titleDesc }}</view>
    <view class="text">{{ titleText }}</view>
  </view>
  <view class="right" wx:if="{{isShowMore}}">
    <view class="btn" bindtap="onClickMore">更多</view>
  </view>
</view>
